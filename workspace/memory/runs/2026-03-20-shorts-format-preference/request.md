# Request

使用者要求把目前的 Shorts 輸出格式與後製標字偏好記錄下來：

- 只要：
  - `Hook Title`
  - `Hook Burst Text`
  - `Template Marked Script`
- 重點字預設用 `「」`
- 只標 `intro` 和章節正文
- 不標 `hook`
- 不標 `[chapter]` 那一行本身
- 不要一整句都標，只標少量真的重要的名詞
